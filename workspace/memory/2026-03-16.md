- 排查并修复了 ClawHub 安装失败 `spawn npm ENOENT`：根因是 `openclaw-gateway` 的 user-level systemd 服务把 PATH 写成了失效的 `/root/.nvm/current/bin`，而真实 npm 在 `/root/.nvm/versions/node/v24.14.0/bin`。已通过 `/root/.config/systemd/user/openclaw-gateway.service.d/override.conf` 覆盖 PATH 并重启服务修复。
- 排查 `nightly-security-audit.sh` 第 13 项“灾备备份”误报：详细报告显示 git commit/push 成功，但摘要仍报失败。根因是 Bash 子 shell `()` 内设置 `BACKUP_STATUS` 导致外层拿不到变量。已按老大要求修改 `/root/.openclaw/workspace/scripts/nightly-security-audit.sh`，把该段从 `()` 改为 `{}`，并验证脚本语法通过、重跑后第 13 项恢复为“✅ 已自动推送至远端仓库”。
- 进一步排查第 5 项“本地 Cron 拉取失败”：现象不是稳定的网关整体故障；`openclaw gateway status`/`openclaw status --deep` 可通，但 `openclaw cron list` 失败并出现 `gateway closed (1000 normal closure)` / `handshake timeout`。后续调查方向已收敛到本机 CLI 使用的身份缺少 `operator.read` scope，而不是简单的 token 失效或端口没开。
- Git 灾备远端链路已确认正常：`origin` 为 `git@github.com:motoabc/openclaw-config.git`，`ssh -T git@github.com` 认证成功，`git push --dry-run origin main` 正常。
